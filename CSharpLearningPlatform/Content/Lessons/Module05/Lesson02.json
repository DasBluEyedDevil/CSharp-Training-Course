{
  "moduleId": 5,
  "lessonNumber": 2,
  "lessonId": "M05L02",
  "title": "Lists (Dynamic Collections)",
  "simplifierConcept": "Arrays have a problem: FIXED SIZE!\n\n❌ Array limitations:\nint[] scores = new int[5];  // Stuck with 5!\n// Want to add 6th item? Can't!\n\n✅ List solution:\nList<int> scores = new List<int>();\nscores.Add(10);  // Add first\nscores.Add(20);  // Add second\nscores.Add(30);  // Keep adding!\nscores.Remove(20);  // Remove items!\n\nList = Array that can GROW and SHRINK!\n\nKey features:\n📦 Dynamic size (grows automatically)\n➕ Add items anytime (.Add)\n➖ Remove items anytime (.Remove)\n🔢 Still indexed (list[0], list[1]...)\n📊 Same type (List<int>, List<string>)\n\nThink: List = Flexible array that expands as needed!",
  "coderExample": "// CREATING LISTS\nusing System.Collections.Generic;  // Need this!\n\n// Empty list\nList<string> names = new List<string>();\n\n// Initialize with values\nList<int> scores = new List<int> { 85, 92, 78, 90 };\n\n// ADDING ITEMS\nnames.Add(\"Alice\");  // First item\nnames.Add(\"Bob\");    // Second item\nnames.Add(\"Charlie\");  // Third item\n// List grows automatically!\n\nConsole.WriteLine($\"Count: {names.Count}\");  // 3\n\n// ACCESSING ITEMS (like array)\nConsole.WriteLine(names[0]);  // \"Alice\"\nConsole.WriteLine(names[1]);  // \"Bob\"\n\n// MODIFYING ITEMS\nnames[1] = \"Robert\";  // Change \"Bob\" to \"Robert\"\n\n// REMOVING ITEMS\nnames.Remove(\"Alice\");  // Removes \"Alice\"\nnames.RemoveAt(0);  // Removes item at index 0\n\n// INSERT AT POSITION\nList<string> fruits = new List<string> { \"Apple\", \"Orange\" };\nfruits.Insert(1, \"Banana\");  // Insert at index 1\n// Now: [\"Apple\", \"Banana\", \"Orange\"]\n\n// CONTAINS - Check if exists\nif (fruits.Contains(\"Banana\"))\n{\n    Console.WriteLine(\"We have bananas!\");\n}\n\n// CLEAR - Remove all\nfruits.Clear();\nConsole.WriteLine($\"Count after clear: {fruits.Count}\");  // 0\n\n// LOOPING THROUGH LIST\nList<int> numbers = new List<int> { 10, 20, 30, 40 };\n\nfor (int i = 0; i < numbers.Count; i++)\n{\n    Console.WriteLine($\"numbers[{i}] = {numbers[i]}\");\n}\n\nforeach (int num in numbers)\n{\n    Console.WriteLine(num);\n}\n\n// PRACTICAL: Shopping list\nList<string> shopping = new List<string>();\n\n// Add items\nshopping.Add(\"Milk\");\nshopping.Add(\"Bread\");\nshopping.Add(\"Eggs\");\nshopping.Add(\"Butter\");\n\nConsole.WriteLine($\"Shopping list has {shopping.Count} items:\");\nforeach (string item in shopping)\n{\n    Console.WriteLine($\"- {item}\");\n}\n\n// Remove item (bought it!)\nshopping.Remove(\"Milk\");\nConsole.WriteLine($\"\\nAfter buying milk: {shopping.Count} items left\");\n\n// PRACTICAL: High scores tracker\nList<int> highScores = new List<int> { 100, 95, 87, 92 };\n\nConsole.WriteLine(\"Enter new score:\");\nint newScore = int.Parse(Console.ReadLine());\n\nhighScores.Add(newScore);\nhighScores.Sort();  // Sort ascending\nhighScores.Reverse();  // Now descending\n\nConsole.WriteLine(\"\\nTop 3 scores:\");\nfor (int i = 0; i < Math.Min(3, highScores.Count); i++)\n{\n    Console.WriteLine($\"{i + 1}. {highScores[i]}\");\n}\n\n// PRACTICAL: Todo list\nList<string> todos = new List<string>();\n\nwhile (true)\n{\n    Console.WriteLine(\"\\n1. Add task\");\n    Console.WriteLine(\"2. Complete task\");\n    Console.WriteLine(\"3. View tasks\");\n    Console.WriteLine(\"4. Exit\");\n    \n    string choice = Console.ReadLine();\n    \n    if (choice == \"1\")\n    {\n        Console.WriteLine(\"Enter task:\");\n        todos.Add(Console.ReadLine());\n    }\n    else if (choice == \"2\" && todos.Count > 0)\n    {\n        Console.WriteLine(\"Task number to complete:\");\n        int index = int.Parse(Console.ReadLine()) - 1;\n        todos.RemoveAt(index);\n    }\n    else if (choice == \"3\")\n    {\n        for (int i = 0; i < todos.Count; i++)\n        {\n            Console.WriteLine($\"{i + 1}. {todos[i]}\");\n        }\n    }\n    else if (choice == \"4\")\n    {\n        break;\n    }\n}",
  "syntaxBreakdown": [
    {
      "codeSnippet": "List<type>",
      "explanation": "List of specific type. List<int> holds integers. List<string> holds strings. <type> is required! Generic syntax - specifies what list contains."
    },
    {
      "codeSnippet": ".Add(item)",
      "explanation": "Adds item to END of list. list.Add(5) appends 5. List automatically grows. Can add unlimited items (memory permitting)."
    },
    {
      "codeSnippet": ".Remove(item)",
      "explanation": "Removes FIRST occurrence of item. list.Remove(5) removes first 5 found. Returns true if found/removed, false if not found."
    },
    {
      "codeSnippet": ".Count vs .Length",
      "explanation": "Lists use .Count (property). Arrays use .Length (property). Both are properties (no parentheses!). list.Count, array.Length."
    }
  ],
  "challenge": {
    "instructions": "Create a contact manager app!\n\n1. Create empty List<string> for names and List<string> for phones\n2. Show menu loop:\n   - Add contact (name + phone)\n   - Remove contact by name\n   - Search contact by name\n   - List all contacts\n   - Exit\n3. For Add: Get name and phone, add to both lists at same index\n4. For Remove: Find index of name, remove from both lists\n5. For Search: Find name, display name and phone\n6. For List: Display all contacts with index numbers\n\nExample:\n=== CONTACT MANAGER ===\n1. Add contact\n2. Remove contact\n3. Search contact\n4. List all\n5. Exit\n\nChoice: 1\nName: Alice\nPhone: 555-1234\nAdded!\n\nChoice: 4\nContacts:\n1. Alice - 555-1234\n\nChoice: 3\nSearch name: Alice\nFound: Alice - 555-1234",
    "starterCode": "// Contact Manager\n\nusing System.Collections.Generic;\n\nList<string> names = new List<string>();\nList<string> phones = new List<string>();\n\nwhile (true)\n{\n    Console.WriteLine(\"\\n=== CONTACT MANAGER ===\");\n    Console.WriteLine(\"1. Add contact\");\n    Console.WriteLine(\"2. Remove contact\");\n    Console.WriteLine(\"3. Search contact\");\n    Console.WriteLine(\"4. List all\");\n    Console.WriteLine(\"5. Exit\");\n    \n    string choice = Console.ReadLine();\n    \n    // Implement menu options\n}",
    "solutionCode": "// Contact Manager\n\nusing System.Collections.Generic;\n\nList<string> names = new List<string>();\nList<string> phones = new List<string>();\n\nwhile (true)\n{\n    Console.WriteLine(\"\\n=== CONTACT MANAGER ===\");\n    Console.WriteLine(\"1. Add contact\");\n    Console.WriteLine(\"2. Remove contact\");\n    Console.WriteLine(\"3. Search contact\");\n    Console.WriteLine(\"4. List all\");\n    Console.WriteLine(\"5. Exit\");\n    \n    string choice = Console.ReadLine();\n    \n    if (choice == \"1\")\n    {\n        // Add contact\n        Console.WriteLine(\"Enter name:\");\n        string name = Console.ReadLine();\n        Console.WriteLine(\"Enter phone:\");\n        string phone = Console.ReadLine();\n        \n        names.Add(name);\n        phones.Add(phone);\n        Console.WriteLine(\"✅ Contact added!\");\n    }\n    else if (choice == \"2\")\n    {\n        // Remove contact\n        Console.WriteLine(\"Enter name to remove:\");\n        string name = Console.ReadLine();\n        \n        int index = names.IndexOf(name);\n        if (index >= 0)\n        {\n            names.RemoveAt(index);\n            phones.RemoveAt(index);\n            Console.WriteLine(\"✅ Contact removed!\");\n        }\n        else\n        {\n            Console.WriteLine(\"❌ Contact not found\");\n        }\n    }\n    else if (choice == \"3\")\n    {\n        // Search contact\n        Console.WriteLine(\"Enter name to search:\");\n        string name = Console.ReadLine();\n        \n        int index = names.IndexOf(name);\n        if (index >= 0)\n        {\n            Console.WriteLine($\"Found: {names[index]} - {phones[index]}\");\n        }\n        else\n        {\n            Console.WriteLine(\"❌ Contact not found\");\n        }\n    }\n    else if (choice == \"4\")\n    {\n        // List all\n        Console.WriteLine($\"\\nContacts ({names.Count}):\");\n        for (int i = 0; i < names.Count; i++)\n        {\n            Console.WriteLine($\"{i + 1}. {names[i]} - {phones[i]}\");\n        }\n    }\n    else if (choice == \"5\")\n    {\n        // Exit\n        Console.WriteLine(\"Goodbye!\");\n        break;\n    }\n}",
    "hint": "Use List<string> for dynamic size. .Add(item) to append. .IndexOf(item) to find position (returns -1 if not found). .RemoveAt(index) to remove by position. Keep two lists synchronized with same index.",
    "expectedOutputPatterns": ["Contact", "Added", "Found", "removed"],
    "validationRules": [],
    "commonStickingPoints": [
      "Forgetting using: List needs using System.Collections.Generic; at top! Without it, List not recognized. Must include this namespace.",
      "Count vs Length: Arrays use .Length. Lists use .Count. list.Length is ERROR! Remember: List = Count, Array = Length. Both are properties (no parentheses).",
      "Remove vs RemoveAt: .Remove(item) removes by VALUE. .RemoveAt(index) removes by POSITION. list.Remove(5) removes value 5. list.RemoveAt(5) removes 6th item!",
      "IndexOf returns -1: list.IndexOf(\\\"Bob\\\") returns -1 if not found! Must check: int i = list.IndexOf(x); if (i >= 0) { ... } before using index."
    ]
  }
}
